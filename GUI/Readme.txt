

The following modules must be installed:
	1) scipy
	2) numpy
	3) matplotlib

To install the modules, install pip (may already be installed) then run the following in the terminal
"pip install scipy"
"pip install numpy"
"pip install matplotlib"

easy.



Note: This script previously ran using matlab engine. All of the code required to plot and analyse the 
      data has now been commented out, but can be reincluded. 
      In order to run this, you must install the matlab engine. Google it.